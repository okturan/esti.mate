{
  "name": "esti-mate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check script.js && node --check calculator.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
